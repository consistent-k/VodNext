---
description: 开发工作流和常用命令
globs: 
alwaysApply: true
---
# 开发工作流

## 常用命令

```bash
# 开发
pnpm dev            # 启动 Next.js 开发服务器
pnpm export:dev     # Tauri 开发模式

# 构建
pnpm build          # 生产构建（standalone 模式）
pnpm export         # 导出静态文件（用于 Tauri）
pnpm tauri build    # 构建桌面应用

# 代码检查
pnpm lint           # ESLint 检查

# 提交
pnpm commit         # 交互式提交（commitizen）
```

## Git 工作流
- **提交格式**: 遵循 Conventional Commits（`feat:`, `fix:`, `docs:`, `refactor:` 等）
- **提交钩子**: Husky + lint-staged 自动运行 ESLint 和 Prettier
- **提交检查**: commitlint 验证提交消息格式

## 新功能开发流程
1. 创建功能分支
2. 在 `app/` 目录创建页面（如需要）
3. 在 `components/` 创建组件
4. 在 `services/` 添加 API 调用
5. 在 `lib/types/` 添加类型定义
6. 运行 `pnpm lint` 检查代码
7. 使用 `pnpm commit` 提交

## 常见任务

### 添加新页面
1. 在 `app/` 创建目录（如 `app/newpage/`）
2. 创建 `page.tsx` 文件
3. 添加 `'use client'` 指令（如需要客户端功能）
4. 创建 `index.module.scss` 样式文件

### 添加新组件
1. 在 `components/` 创建目录（如 `components/video/NewComponent/`）
2. 创建 `index.tsx` 文件
3. 定义 Props 接口并导出
4. 创建 `index.module.scss` 样式文件
5. 使用 `export default` 导出组件

### 添加新 API
1. 在 `lib/types/` 定义请求/响应类型
2. 在 `services/vodhub/` 添加 API 函数
3. 使用 `request.get<T>` 或 `request.post<T>` 调用
4. 在 `services/index.ts` 导出新函数

### 添加新状态
1. 在 `lib/store/` 创建 `use{Name}Store.ts`
2. 定义接口和状态类型
3. 使用 Zustand `create` 创建 store
4. 添加 `persist` 中间件（如需要持久化）

## 调试技巧
- **浏览器控制台**: 使用 `console.log` 调试
- **网络请求**: 检查 `/api/vodhub/` 代理请求
- **Tauri 环境**: 检查 `window.__TAURI__` 对象
- **状态检查**: 使用 React DevTools 或 Zustand DevTools

## 性能优化
- 使用 `React.memo` 避免不必要的重渲染
- 使用 `useMemo` 和 `useCallback` 缓存计算和函数
- 使用 `dynamic` 导入大型组件（如视频播放器）
- 使用 `Suspense` 处理异步加载

## 注意事项
- **Tauri 兼容性**: 使用 `@tauri-apps/plugin-http` 替代 axios（桌面端）
- **SSR 兼容性**: 客户端组件添加 `'use client'` 指令
- **主题支持**: 样式需要支持亮色/暗色主题
- **响应式设计**: 使用 Ant Design 的 `Flex` 和 `Row/Col` 组件
